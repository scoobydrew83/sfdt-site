---
title: Web Dashboard (sfdt ui)
description: The local Lightning Design System dashboard — every page, live SSE command runners, the AI chat drawer, and its security model.
---

import { Callout } from 'nextra/components'
import { GuiPageTable, SurfaceCount } from '../../components/catalogs'

# Web Dashboard

`sfdt ui` starts a local Express server and opens a **Salesforce Lightning Design System**
dashboard in your browser. It reads log/result files from your project's `logDir` (default
`logs/`) and can run several commands live.

```bash
sfdt ui                   # opens http://localhost:7654
sfdt ui --port 8080       # custom port
sfdt ui --no-open         # start without opening the browser
```

<Callout type="info">
  When installing from source, build the dashboard once with `npm run build:gui`. The published
  npm package already includes a pre-built `gui/dist/`, so end users don't need to build it. If
  `gui/dist/` is missing, `sfdt ui` shows a build-instructions page instead.
</Callout>

## Pages

All <SurfaceCount surface="gui" metric="pages" /> pages, in nav order — the page list is
generated from the dashboard's own route registry, so it cannot drift from the app:

<GuiPageTable details={{
  dashboard: { shows: 'Summary stat cards: last test run, preflight, drift', source: 'preflight-latest.json, drift-latest.json, test-results/' },
  drift: { shows: 'Filterable component table (All / Clean / Drift); run inline', source: 'drift-latest.json' },
  audit: { shows: 'Org health: audit trail, licenses, MFA, unused Apex, API versions; Run now (SSE)', source: 'audit-latest.json' },
  monitor: { shows: 'Limits, errors, health score, backup history; Run now (SSE)', source: 'monitor-latest.json' },
  tests: { shows: 'Apex test history with coverage bands; run tests inline', source: 'test-results/*.json' },
  coverage: { shows: 'Live org-wide Apex coverage (with threshold band) plus the per-run coverage trend', source: '/api/coverage, test-results/' },
  logs: { shows: 'Searchable log viewer with pagination and raw output', source: 'logs/*' },
  release: { shows: 'Manifest picker, changelog & release-note editing, deploy', source: 'manifest/release/, release-notes/, CHANGELOG.md' },
  retrofit: { shows: 'Cross-org retrofit: retrieve from a source org, commit, smart-deploy to a target (validate-only or execute)', source: '/api/retrofit' },
  compare: { shows: 'Source-only / target-only / both; side-by-side XML diff; export package.xml', source: 'compare-latest.json' },
  scan: { shows: 'Full metadata inventory with search & filter', source: '/api/scan' },
  preflight: { shows: 'Per-check pass/warn/fail; run preflight inline', source: 'preflight-latest.json' },
  manifests: { shows: 'Generated package.xml artifacts — preview & download', source: 'manifest/release/*.xml' },
  quality: { shows: 'Code/test quality results; stub generator with dry-run preview', source: 'quality-latest.json' },
  'agent-test': { shows: 'Agentforce agent-test runs (AiEvaluationDefinition) with pass/fail history', source: '/api/agent-test' },
  pull: { shows: 'Smart-delta / preview / full retrieve with live streaming', source: '/api/pull (SSE)' },
  review: { shows: 'AI code-review results across five categories', source: 'review-latest.json' },
  explain: { shows: 'AI deployment-log analysis', source: 'explain-latest.json' },
  flows: { shows: 'Flow health scores, trigger conflicts, subflow graph', source: '/api/flow/quality' },
  dependency: { shows: 'Seed + expand-on-click D3 dependency graph (references / referenced-by, capped 50/direction); Gaps toggle for source-parsed references the Tooling API misses; Show-inferred overlay renders missing edges as dashed lines', source: '/api/dependencies/resolve, /api/dependencies/neighbors, /api/dependencies/gaps' },
  scratch: { shows: 'Active scratch orgs, plus create / delete / pool-fill actions', source: '/api/scratch' },
  data: { shows: 'Defined sf data tree data sets, plus export / import / delete actions', source: '/api/data' },
  docs: { shows: 'Docs config, plus a generate action', source: '/api/docs' },
  notifications: { shows: 'Notification channels (redacted), per-channel event filters, and a send-test action', source: '/api/notifications' },
  settings: { shows: 'View/edit config; initialize a project from the GUI', source: '.sfdt/config.json' },
}} />

Data appears automatically after running the corresponding CLI commands (`sfdt test`,
`sfdt preflight`, `sfdt drift`, …) that write JSON result files.

## Live command runners

The Test Runs, Preflight, Drift, Compare, Pull, **Audit**, and **Monitor** pages have **Run**
buttons that trigger the underlying command over a Server-Sent Events stream — output appears
line-by-line in real time, exactly like running the CLI.

The Scratch (create / delete / pool-fill), Data (export / import / delete), and Docs (generate)
pages add real mutating actions behind in-app confirmation dialogs — which also work inside the
VS Code-embedded dashboard (no `window.confirm`).

## AI chat drawer

The toolbar's **Ask AI** button opens a sliding chat panel. Pages with relevant output (Review,
Explain, Drift, Preflight) pre-fill the chat with that context so you can ask follow-ups without
copy-pasting.

## Security

<Callout type="warning">
  The dashboard is local-only and hardened against page-driven attacks:
</Callout>

- **Binds to `127.0.0.1`** — never exposed to the network.
- **Per-launch auth token** prevents unauthorized access to a running process.
- **CSRF protection** — all mutating routes require an `X-SFDT-CSRF` header.
- **Read-only AI sandbox** — the AI chat's tool access is restricted to `Read,Grep,Glob` (no
  Bash/Write/Edit), so a prompt injection in page context can't drive tool execution.
- **Log redaction at ingest** — secrets are redacted before reaching the in-memory buffer or the
  live SSE stream.

The same bridge endpoints the dashboard exposes are what the Chrome extension talks to — see
[The Bridge](/guides/the-bridge).
